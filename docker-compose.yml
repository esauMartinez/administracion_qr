version: '3.9'
services:
  sistema-prueba:
    build: .
    restart: always
    # container_name: front_sistema_prueba_v2
    ports:
      - '3300:3300'
    networks:
      - network-apis

networks:
  network-apis:
    name: network-apis
    external: true
